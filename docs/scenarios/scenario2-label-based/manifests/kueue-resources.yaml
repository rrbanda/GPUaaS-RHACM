# Scenario 2: GPU Queue Resources
# Uses label-based Placement for GPU cluster selection
#
# IMPORTANT: The ClusterQueue and LocalQueue names MUST match what the
# kueue-addon syncs to spoke clusters (configured in kueue-addon-values.yaml).
# The addon syncs: cluster-queue and user-queue by default.
#
# If you need different queue names:
# 1. Update kueue-addon-values.yaml with your custom queue names
# 2. helm upgrade kueue-addon ... -f kueue-addon-values.yaml
---
apiVersion: kueue.x-k8s.io/v1beta1
kind: ClusterQueue
metadata:
  name: "cluster-queue"  # Must match addon's clusterQueue.name
spec:
  namespaceSelector: {}
  resourceGroups:
  - coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
    flavors:
    - name: "default-flavor"
      resources:
      - name: "cpu"
        nominalQuota: 32
      - name: "memory"
        nominalQuota: 128Gi
      - name: "nvidia.com/gpu"
        nominalQuota: 4
  admissionChecks:
  - multikueue-gpu
  - gpu-placement-check
---
apiVersion: kueue.x-k8s.io/v1beta1
kind: LocalQueue
metadata:
  namespace: "default"
  name: "user-queue"  # Must match addon's localQueue.name
spec:
  clusterQueue: "cluster-queue"
---
apiVersion: kueue.x-k8s.io/v1beta1
kind: AdmissionCheck
metadata:
  name: multikueue-gpu
spec:
  controllerName: kueue.x-k8s.io/multikueue
  parameters:
    apiGroup: kueue.x-k8s.io
    kind: MultiKueueConfig
    name: gpu-placement
---
# OCM implements an AdmissionCheck controller that watches Placements
# and auto-generates MultiKueueConfig based on placement decisions
apiVersion: kueue.x-k8s.io/v1beta1
kind: AdmissionCheck
metadata:
  name: gpu-placement-check
spec:
  controllerName: open-cluster-management.io/placement
  parameters:
    apiGroup: cluster.open-cluster-management.io
    kind: Placement
    name: gpu-placement
